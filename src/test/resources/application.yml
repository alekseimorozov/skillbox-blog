spring:
  jpa:
    defer-datasource-initialization: true
    show-sql: true
    properties:
      hibernate:
        generate_statistics: true
        format_sql: true
logging:
  level:
    org:
      hibernate:
        stat: debug
        type: trace
